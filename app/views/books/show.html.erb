<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t("views.action.edit"), edit_book_path(@book) %> |
  <%= link_to t("views.action.back"), books_path %>
  <%= button_to t("views.action.delete"), @book, method: :delete %>
</nav>
